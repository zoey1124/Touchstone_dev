T[account_aliases; 2000; id, integer; account_id, integer; acct, varchar; uri, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[account_conversations; 2000; id, integer; account_id, integer; conversation_id, integer; participant_account_ids, varchar; status_ids, varchar; last_status_id, integer; lock_version, integer; unread, bool; p(id)]
T[account_deletion_requests; 2000; id, integer; account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[account_domain_blocks; 2000; id, integer; domain, varchar; created_at, datetime; updated_at, datetime; account_id, integer; p(id)]
T[account_migrations; 2000; id, integer; account_id, integer; acct, varchar; followers_count, integer; target_account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[account_moderation_notes; 2000; id, integer; content, varchar; account_id, integer; target_account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[account_notes; 2000; id, integer; account_id, integer; target_account_id, integer; comment, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[account_pins; 2000; id, integer; account_id, integer; target_account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[account_stats; 2000; id, integer; account_id, integer; statuses_count, integer; following_count, integer; followers_count, integer; created_at, datetime; updated_at, datetime; last_status_at, datetime; p(id)]
T[account_statuses_cleanup_policies; 2000; id, integer; account_id, integer; enabled, bool; min_status_age, integer; keep_direct, bool; keep_pinned, bool; keep_polls, bool; keep_media, bool; keep_self_fav, bool; keep_self_bookmark, bool; min_favs, integer; min_reblogs, integer; created_at, datetime; updated_at, datetime; p(id)]
T[accounts; 2000; id, integer; username, varchar; domain, varchar; private_key, varchar; public_key, varchar; created_at, datetime; updated_at, datetime; note, varchar; display_name, varchar; uri, varchar; url, varchar; avatar_file_name, varchar; avatar_content_type, varchar; avatar_file_size, integer; avatar_updated_at, datetime; header_file_name, varchar; header_content_type, varchar; header_file_size, integer; header_updated_at, datetime; avatar_remote_url, varchar; locked, bool; header_remote_url, varchar; last_webfingered_at, datetime; inbox_url, varchar; outbox_url, varchar; shared_inbox_url, varchar; followers_url, varchar; protocol, integer; memorial, bool; moved_to_account_id, integer; featured_collection_url, varchar; fields, varchar; actor_type, varchar; discoverable, bool; also_known_as, varchar; silenced_at, datetime; suspended_at, datetime; hide_collections, bool; avatar_storage_schema_version, integer; header_storage_schema_version, integer; devices_url, varchar; suspension_origin, integer; sensitized_at, datetime; trendable, bool; reviewed_at, datetime; requested_review_at, datetime; p(id)]
T[statuses; 2000; id, integer; uri, varchar; text, varchar; created_at, datetime; updated_at, datetime; in_reply_to_id, integer; reblog_of_id, integer; url, varchar; "sensitive", bool; visibility, integer; spoiler_text, varchar; reply, bool; "language", varchar; conversation_id, integer; "local", bool; account_id, integer; application_id, integer; in_reply_to_account_id, integer; poll_id, integer; deleted_at, datetime; edited_at, datetime; trendable, bool; ordered_media_attachment_ids, varchar; p(id)]
T[account_warning_presets; 2000; id, integer; text, varchar; created_at, datetime; updated_at, datetime; title, varchar; p(id)]
T[account_warnings; 2000; id, integer; account_id, integer; target_account_id, integer; action, integer; text, varchar; created_at, datetime; updated_at, datetime; report_id, integer; status_ids, varchar; overruled_at, datetime; p(id)]
T[accounts_tags; 2000; account_id, integer; tag_id, integer; p(id)]
T[admin_action_logs; 2000; id, integer; account_id, integer; action, varchar; target_type, varchar; target_id, integer; recorded_changes, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[announcement_mutes; 2000; id, integer; account_id, integer; announcement_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[announcement_reactions; 2000; id, integer; account_id, integer; announcement_id, integer; name, varchar; custom_emoji_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[announcements; 2000; id, integer; text, varchar; published, bool; all_day, bool; scheduled_at, datetime; starts_at, datetime; ends_at, datetime; created_at, datetime; updated_at, datetime; published_at, datetime; status_ids, varchar; p(id)]
T[appeals; 2000; id, integer; account_id, integer; account_warning_id, integer; text, varchar; approved_at, datetime; approved_by_account_id, integer; rejected_at, datetime; rejected_by_account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[ar_internal_metadata; 2000; "key", varchar; "value", varchar; created_at, datetime; updated_at, datetime; p(id)]
T[backups; 2000; id, integer; user_id, integer; dump_file_name, varchar; dump_content_type, varchar; dump_updated_at, datetime; processed, bool; created_at, datetime; updated_at, datetime; dump_file_size, integer; p(id)]
T[blocks; 2000; id, integer; created_at, datetime; updated_at, datetime; account_id, integer; target_account_id, integer; uri, varchar; p(id)]
T[bookmarks; 2000; id, integer; account_id, integer; status_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[canonical_email_blocks; 2000; id, integer; canonical_email_hash, varchar; reference_account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[conversation_mutes; 2000; id, integer; conversation_id, integer; account_id, integer; p(id)]
T[conversations; 2000; id, integer; uri, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[custom_emoji_categories; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[custom_emojis; 2000; id, integer; shortcode, varchar; domain, varchar; image_file_name, varchar; image_content_type, varchar; image_file_size, integer; image_updated_at, datetime; created_at, datetime; updated_at, datetime; disabled, bool; uri, varchar; image_remote_url, varchar; visible_in_picker, bool; category_id, integer; image_storage_schema_version, integer; p(id)]
T[custom_filters; 2000; id, integer; account_id, integer; expires_at, datetime; phrase, varchar; context, varchar; irreversible, bool; created_at, datetime; updated_at, datetime; whole_word, bool; p(id)]
T[devices; 2000; id, integer; access_token_id, integer; account_id, integer; device_id, varchar; name, varchar; fingerprint_key, varchar; identity_key, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[domain_allows; 2000; id, integer; domain, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[domain_blocks; 2000; id, integer; domain, varchar; created_at, datetime; updated_at, datetime; severity, integer; reject_media, bool; reject_reports, bool; private_comment, varchar; public_comment, varchar; obfuscate, bool; p(id)]
T[email_domain_blocks; 2000; id, integer; domain, varchar; created_at, datetime; updated_at, datetime; parent_id, integer; p(id)]
T[encrypted_messages; 2000; id, integer; device_id, integer; from_account_id, integer; from_device_id, varchar; type, integer; body, varchar; digest, varchar; message_franking, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[favourites; 2000; id, integer; created_at, datetime; updated_at, datetime; account_id, integer; status_id, integer; p(id)]
T[featured_tags; 2000; id, integer; account_id, integer; tag_id, integer; statuses_count, integer; last_status_at, datetime; created_at, datetime; updated_at, datetime; p(id)]
T[follow_recommendation_suppressions; 2000; id, integer; account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[follows; 2000; id, integer; created_at, datetime; updated_at, datetime; account_id, integer; target_account_id, integer; show_reblogs, bool; uri, varchar; notify, bool; p(id)]
T[status_stats; 2000; id, integer; status_id, integer; replies_count, integer; reblogs_count, integer; favourites_count, integer; created_at, datetime; updated_at, datetime; p(id)]
T[users; 2000; id, integer; email, varchar; created_at, datetime; updated_at, datetime; encrypted_password, varchar; reset_password_token, varchar; reset_password_sent_at, datetime; sign_in_count, integer; current_sign_in_at, datetime; last_sign_in_at, datetime; admin, bool; confirmation_token, varchar; confirmed_at, datetime; confirmation_sent_at, datetime; unconfirmed_email, varchar; locale, varchar; encrypted_otp_secret, varchar; encrypted_otp_secret_iv, varchar; encrypted_otp_secret_salt, varchar; consumed_timestep, integer; otp_required_for_login, bool; last_emailed_at, datetime; otp_backup_codes, varchar; filtered_languages, varchar; account_id, integer; disabled, bool; moderator, bool; invite_id, integer; chosen_languages, varchar; created_by_application_id, integer; approved, bool; sign_in_token, varchar; sign_in_token_sent_at, datetime; webauthn_id, varchar; sign_up_ip, varchar; skip_sign_in_token, bool; p(id)]
T[follow_requests; 2000; id, integer; created_at, datetime; updated_at, datetime; account_id, integer; target_account_id, integer; show_reblogs, bool; uri, varchar; notify, bool; p(id)]
T[identities; 2000; id, integer; provider, varchar; uid, varchar; created_at, datetime; updated_at, datetime; user_id, integer; p(id)]
T[imports; 2000; id, integer; type, integer; approved, bool; created_at, datetime; updated_at, datetime; data_file_name, varchar; data_content_type, varchar; data_file_size, integer; data_updated_at, datetime; account_id, integer; overwrite, bool; p(id)]
T[invites; 2000; id, integer; user_id, integer; code, varchar; expires_at, datetime; max_uses, integer; uses, integer; created_at, datetime; updated_at, datetime; autofollow, bool; comment, varchar; p(id)]
T[ip_blocks; 2000; id, integer; created_at, datetime; updated_at, datetime; expires_at, datetime; ip, varchar; severity, integer; comment, varchar; p(id)]
T[list_accounts; 2000; id, integer; list_id, integer; account_id, integer; follow_id, integer; p(id)]
T[lists; 2000; id, integer; account_id, integer; title, varchar; created_at, datetime; updated_at, datetime; replies_policy, integer; p(id)]
T[login_activities; 2000; id, integer; user_id, integer; authentication_method, varchar; provider, varchar; success, bool; failure_reason, varchar; ip, varchar; user_agent, varchar; created_at, datetime; p(id)]
T[markers; 2000; id, integer; user_id, integer; timeline, varchar; last_read_id, integer; lock_version, integer; created_at, datetime; updated_at, datetime; p(id)]
T[media_attachments; 2000; id, integer; status_id, integer; file_file_name, varchar; file_content_type, varchar; file_file_size, integer; file_updated_at, datetime; remote_url, varchar; created_at, datetime; updated_at, datetime; shortcode, varchar; type, integer; file_meta, varchar; account_id, integer; description, varchar; scheduled_status_id, integer; blurhash, varchar; processing, integer; file_storage_schema_version, integer; thumbnail_file_name, varchar; thumbnail_content_type, varchar; thumbnail_file_size, integer; thumbnail_updated_at, datetime; thumbnail_remote_url, varchar; p(id)]
T[mentions; 2000; id, integer; status_id, integer; created_at, datetime; updated_at, datetime; account_id, integer; silent, bool; p(id)]
T[mutes; 2000; id, integer; created_at, datetime; updated_at, datetime; hide_notifications, bool; account_id, integer; target_account_id, integer; expires_at, datetime; p(id)]
T[notifications; 2000; id, integer; activity_id, integer; activity_type, varchar; created_at, datetime; updated_at, datetime; account_id, integer; from_account_id, integer; type, varchar; p(id)]
T[oauth_access_grants; 2000; id, integer; token, varchar; expires_in, integer; redirect_uri, varchar; created_at, datetime; revoked_at, datetime; scopes, varchar; application_id, integer; resource_owner_id, integer; p(id)]
T[oauth_access_tokens; 2000; id, integer; token, varchar; refresh_token, varchar; expires_in, integer; revoked_at, datetime; created_at, datetime; scopes, varchar; application_id, integer; resource_owner_id, integer; last_used_at, datetime; last_used_ip, varchar; p(id)]
T[oauth_applications; 2000; id, integer; name, varchar; uid, varchar; secret, varchar; redirect_uri, varchar; scopes, varchar; created_at, datetime; updated_at, datetime; superapp, bool; website, varchar; owner_type, varchar; owner_id, integer; confidential, bool; p(id)]
T[one_time_keys; 2000; id, integer; device_id, integer; key_id, varchar; "key", varchar; signature, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[pghero_space_stats; 2000; id, integer; database, varchar; schema, varchar; relation, varchar; size, integer; captured_at, datetime; p(id)]
T[poll_votes; 2000; id, integer; account_id, integer; poll_id, integer; choice, integer; created_at, datetime; updated_at, datetime; uri, varchar; p(id)]
T[polls; 2000; id, integer; account_id, integer; status_id, integer; expires_at, datetime; options, varchar; cached_tallies, varchar; multiple, bool; hide_totals, bool; votes_count, integer; last_fetched_at, datetime; created_at, datetime; updated_at, datetime; lock_version, integer; voters_count, integer; p(id)]
T[preview_card_providers; 2000; id, integer; domain, varchar; icon_file_name, varchar; icon_content_type, varchar; icon_file_size, integer; icon_updated_at, datetime; trendable, bool; reviewed_at, datetime; requested_review_at, datetime; created_at, datetime; updated_at, datetime; p(id)]
T[preview_cards; 2000; id, integer; url, varchar; title, varchar; description, varchar; image_file_name, varchar; image_content_type, varchar; image_file_size, integer; image_updated_at, datetime; type, integer; html, varchar; author_name, varchar; author_url, varchar; provider_name, varchar; provider_url, varchar; width, integer; height, integer; created_at, datetime; updated_at, datetime; embed_url, varchar; image_storage_schema_version, integer; blurhash, varchar; "language", varchar; max_score, decimal; max_score_at, datetime; trendable, bool; link_type, integer; p(id)]
T[preview_cards_statuses; 2000; preview_card_id, integer; status_id, integer; p(id)]
T[relays; 2000; id, integer; inbox_url, varchar; follow_activity_id, varchar; created_at, datetime; updated_at, datetime; "state", integer; p(id)]
T[report_notes; 2000; id, integer; content, varchar; report_id, integer; account_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[reports; 2000; id, integer; status_ids, varchar; comment, varchar; created_at, datetime; updated_at, datetime; account_id, integer; action_taken_by_account_id, integer; target_account_id, integer; assigned_account_id, integer; uri, varchar; forwarded, bool; category, integer; action_taken_at, datetime; rule_ids, varchar; p(id)]
T[rules; 2000; id, integer; priority, integer; deleted_at, datetime; text, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[scheduled_statuses; 2000; id, integer; account_id, integer; scheduled_at, datetime; params, varchar; p(id)]
T[schema_migrations; 2000; version, varchar; p(id)]
T[session_activations; 2000; id, integer; session_id, varchar; created_at, datetime; updated_at, datetime; user_agent, varchar; ip, varchar; access_token_id, integer; user_id, integer; web_push_subscription_id, integer; p(id)]
T[settings; 2000; id, integer; var, varchar; "value", varchar; thing_type, varchar; created_at, datetime; updated_at, datetime; thing_id, integer; p(id)]
T[site_uploads; 2000; id, integer; var, varchar; file_file_name, varchar; file_content_type, varchar; file_file_size, integer; file_updated_at, datetime; meta, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[status_edits; 2000; id, integer; status_id, integer; account_id, integer; text, varchar; spoiler_text, varchar; created_at, datetime; updated_at, datetime; ordered_media_attachment_ids, varchar; media_descriptions, varchar; poll_options, varchar; "sensitive", bool; p(id)]
T[status_pins; 2000; id, integer; account_id, integer; status_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[statuses_tags; 2000; status_id, integer; tag_id, integer; p(id)]
T[system_keys; 2000; id, integer; "key", varchar; created_at, datetime; updated_at, datetime; p(id)]
T[tags; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; usable, bool; trendable, bool; listable, bool; reviewed_at, datetime; requested_review_at, datetime; last_status_at, datetime; max_score, decimal; max_score_at, datetime; p(id)]
T[tombstones; 2000; id, integer; account_id, integer; uri, varchar; created_at, datetime; updated_at, datetime; by_moderator, bool; p(id)]
T[unavailable_domains; 2000; id, integer; domain, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[user_invite_requests; 2000; id, integer; user_id, integer; text, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[web_push_subscriptions; 2000; id, integer; endpoint, varchar; key_p256dh, varchar; key_auth, varchar; data, varchar; created_at, datetime; updated_at, datetime; access_token_id, integer; user_id, integer; p(id)]
T[web_settings; 2000; id, integer; data, varchar; created_at, datetime; updated_at, datetime; user_id, integer; p(id)]
D[account_statuses_cleanup_policies.min_status_age; 0; 15; 100; (); ]
D[login_activities.authentication_method; 0; 15; 100; (); ]
D[markers.timeline; 0; 15; 100; (); ]
D[notifications.type; 0; 15; 100; (); ]
D[notifications.activity_type; 0; 15; 100; ('Favourite', 'Follow', 'FollowRequest', 'Mention', 'Poll', 'Status'); ]
