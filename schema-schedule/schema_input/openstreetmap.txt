T[acls; 2000; id, integer; address, varchar; k, varchar; v, varchar; domain, varchar; mx, varchar; p(id)]
T[active_storage_attachments; 2000; id, integer; name, varchar; record_type, varchar; record_id, integer; blob_id, integer; created_at, datetime; p(id)]
T[active_storage_blobs; 2000; id, integer; "key", varchar; filename, varchar; content_type, varchar; metadata, varchar; byte_size, integer; checksum, varchar; created_at, datetime; service_name, varchar; p(id)]
T[active_storage_variant_records; 2000; id, integer; blob_id, integer; variation_digest, varchar; p(id)]
T[ar_internal_metadata; 2000; "key", varchar; "value", varchar; created_at, datetime; updated_at, datetime; p(id)]
T[changeset_comments; 2000; id, integer; changeset_id, integer; author_id, integer; body, varchar; created_at, datetime; visible, bool; p(id)]
T[changeset_tags; 2000; changeset_id, integer; k, varchar; v, varchar; p(id)]
T[changesets; 2000; id, integer; user_id, integer; created_at, datetime; min_lat, integer; max_lat, integer; min_lon, integer; max_lon, integer; closed_at, datetime; num_changes, integer; p(id)]
T[changesets_subscribers; 2000; subscriber_id, integer; changeset_id, integer; p(id)]
T[client_applications; 2000; id, integer; name, varchar; url, varchar; support_url, varchar; callback_url, varchar; "key", varchar; secret, varchar; user_id, integer; created_at, datetime; updated_at, datetime; allow_read_prefs, bool; allow_write_prefs, bool; allow_write_diary, bool; allow_write_api, bool; allow_read_gpx, bool; allow_write_gpx, bool; allow_write_notes, bool; p(id)]
T[current_node_tags; 2000; node_id, integer; k, varchar; v, varchar; p(id)]
T[current_nodes; 2000; id, integer; latitude, integer; longitude, integer; changeset_id, integer; visible, bool; "timestamp", datetime; tile, integer; version, integer; p(id)]
T[current_relation_members; 2000; relation_id, integer; member_type, varchar; member_id, integer; member_role, varchar; sequence_id, integer; p(id)]
T[current_relation_tags; 2000; relation_id, integer; k, varchar; v, varchar; p(id)]
T[current_relations; 2000; id, integer; changeset_id, integer; "timestamp", datetime; visible, bool; version, integer; p(id)]
T[current_way_nodes; 2000; way_id, integer; node_id, integer; sequence_id, integer; p(id)]
T[current_way_tags; 2000; way_id, integer; k, varchar; v, varchar; p(id)]
T[current_ways; 2000; id, integer; changeset_id, integer; "timestamp", datetime; visible, bool; version, integer; p(id)]
T[delayed_jobs; 2000; id, integer; priority, integer; attempts, integer; handler, varchar; last_error, varchar; run_at, datetime; locked_at, datetime; failed_at, datetime; locked_by, varchar; queue, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[diary_comments; 2000; id, integer; diary_entry_id, integer; user_id, integer; body, varchar; created_at, datetime; updated_at, datetime; visible, bool; body_format, varchar; p(id)]
T[diary_entries; 2000; id, integer; user_id, integer; title, varchar; body, varchar; created_at, datetime; updated_at, datetime; latitude, decimal; longitude, decimal; language_code, varchar; visible, bool; body_format, varchar; p(id)]
T[diary_entry_subscriptions; 2000; user_id, integer; diary_entry_id, integer; p(id)]
T[friends; 2000; id, integer; user_id, integer; friend_user_id, integer; created_at, datetime; p(id)]
T[gps_points; 2000; altitude, decimal; trackid, integer; latitude, integer; longitude, integer; gpx_id, integer; "timestamp", datetime; tile, integer; p(id)]
T[gpx_file_tags; 2000; gpx_id, integer; tag, varchar; id, integer; p(id)]
T[gpx_files; 2000; id, integer; user_id, integer; visible, bool; name, varchar; size, integer; latitude, decimal; longitude, decimal; "timestamp", datetime; description, varchar; inserted, bool; visibility, varchar; p(id)]
T[issue_comments; 2000; id, integer; issue_id, integer; user_id, integer; body, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[issues; 2000; id, integer; reportable_type, varchar; reportable_id, integer; reported_user_id, integer; status, varchar; assigned_role, varchar; resolved_at, datetime; resolved_by, integer; updated_by, integer; reports_count, integer; created_at, datetime; updated_at, datetime; p(id)]
T[languages; 2000; code, varchar; english_name, varchar; native_name, varchar; p(id)]
T[messages; 2000; id, integer; from_user_id, integer; title, varchar; body, varchar; sent_on, datetime; message_read, bool; to_user_id, integer; to_user_visible, bool; from_user_visible, bool; body_format, varchar; p(id)]
T[node_tags; 2000; node_id, integer; version, integer; k, varchar; v, varchar; p(id)]
T[nodes; 2000; node_id, integer; latitude, integer; longitude, integer; changeset_id, integer; visible, bool; "timestamp", datetime; tile, integer; version, integer; redaction_id, integer; p(id)]
T[note_comments; 2000; id, integer; note_id, integer; visible, bool; created_at, datetime; author_ip, varchar; author_id, integer; body, varchar; event, varchar; p(id)]
T[notes; 2000; id, integer; latitude, integer; longitude, integer; tile, integer; updated_at, datetime; created_at, datetime; status, varchar; closed_at, datetime; p(id)]
T[oauth_access_grants; 2000; id, integer; resource_owner_id, integer; application_id, integer; token, varchar; expires_in, integer; redirect_uri, varchar; created_at, datetime; revoked_at, datetime; scopes, varchar; code_challenge, varchar; code_challenge_method, varchar; p(id)]
T[oauth_access_tokens; 2000; id, integer; resource_owner_id, integer; application_id, integer; token, varchar; refresh_token, varchar; expires_in, integer; revoked_at, datetime; created_at, datetime; scopes, varchar; previous_refresh_token, varchar; p(id)]
T[oauth_applications; 2000; id, integer; owner_type, varchar; owner_id, integer; name, varchar; uid, varchar; secret, varchar; redirect_uri, varchar; scopes, varchar; confidential, bool; created_at, datetime; updated_at, datetime; p(id)]
T[oauth_nonces; 2000; id, integer; nonce, varchar; "timestamp", integer; created_at, datetime; updated_at, datetime; p(id)]
T[oauth_tokens; 2000; id, integer; user_id, integer; type, varchar; client_application_id, integer; token, varchar; secret, varchar; authorized_at, datetime; invalidated_at, datetime; created_at, datetime; updated_at, datetime; allow_read_prefs, bool; allow_write_prefs, bool; allow_write_diary, bool; allow_write_api, bool; allow_read_gpx, bool; allow_write_gpx, bool; callback_url, varchar; verifier, varchar; "scope", varchar; valid_to, datetime; allow_write_notes, bool; p(id)]
T[redactions; 2000; id, integer; title, varchar; description, varchar; created_at, datetime; updated_at, datetime; user_id, integer; description_format, varchar; p(id)]
T[relation_members; 2000; relation_id, integer; member_type, varchar; member_id, integer; member_role, varchar; version, integer; sequence_id, integer; p(id)]
T[relation_tags; 2000; relation_id, integer; k, varchar; v, varchar; version, integer; p(id)]
T[relations; 2000; relation_id, integer; changeset_id, integer; "timestamp", datetime; version, integer; visible, bool; redaction_id, integer; p(id)]
T[reports; 2000; id, integer; issue_id, integer; user_id, integer; details, varchar; category, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[schema_migrations; 2000; version, varchar; p(id)]
T[user_blocks; 2000; id, integer; user_id, integer; creator_id, integer; reason, varchar; ends_at, datetime; needs_view, bool; revoker_id, integer; created_at, datetime; updated_at, datetime; reason_format, varchar; p(id)]
T[user_preferences; 2000; user_id, integer; k, varchar; v, varchar; p(id)]
T[user_roles; 2000; id, integer; user_id, integer; "role", varchar; created_at, datetime; updated_at, datetime; granter_id, integer; p(id)]
T[user_tokens; 2000; id, integer; user_id, integer; token, varchar; expiry, datetime; referer, varchar; p(id)]
T[users; 2000; email, varchar; id, integer; pass_crypt, varchar; creation_time, datetime; display_name, varchar; data_public, bool; description, varchar; home_lat, decimal; home_lon, decimal; home_zoom, integer; pass_salt, varchar; email_valid, bool; new_email, varchar; creation_ip, varchar; languages, varchar; status, varchar; terms_agreed, datetime; consider_pd, bool; auth_uid, varchar; preferred_editor, varchar; terms_seen, bool; description_format, varchar; changesets_count, integer; traces_count, integer; diary_entries_count, integer; image_use_gravatar, bool; auth_provider, varchar; home_tile, integer; tou_agreed, datetime; p(id)]
T[way_nodes; 2000; way_id, integer; node_id, integer; version, integer; sequence_id, integer; p(id)]
T[way_tags; 2000; way_id, integer; k, varchar; v, varchar; version, integer; p(id)]
D[changeset_comments.visible; 0; 15; 100; (); ]
D[current_nodes.visible; 0; 15; 100; (); ]
D[note_comments.visible; 0; 15; 100; (); ]
D[oauth_tokens.type; 0; 15; 100; ('OauthToken', 'AccessToken', 'Oauth2Token', 'Oauth2Verifier', 'RequestToken'); ]
D[nodes.visible; 0; 15; 100; (); ]
D[relations.visible; 0; 15; 100; (); ]
D[ways.visible; 0; 15; 100; (); ]
D[current_relations.visible; 0; 15; 100; (); ]
D[users.home_lat; 0; 15; 100; (); ]
D[users.home_lon; 0; 15; 100; (); ]
D[users.preferred_editor; 0; 15; 100; (); ]
D[current_ways.visible; 0; 15; 100; (); ]
