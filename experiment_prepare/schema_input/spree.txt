T[action_mailbox_inbound_emails; 2000; id, integer; status, integer; message_id, varchar; message_checksum, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[action_text_rich_texts; 2000; id, integer; name, varchar; body, varchar; record_type, varchar; record_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[active_storage_attachments; 2000; id, integer; name, varchar; record_type, varchar; record_id, integer; blob_id, integer; created_at, datetime; p(id)]
T[active_storage_blobs; 2000; id, integer; "key", varchar; filename, varchar; content_type, varchar; metadata, varchar; service_name, varchar; byte_size, integer; checksum, varchar; created_at, datetime; p(id)]
T[active_storage_variant_records; 2000; id, integer; blob_id, integer; variation_digest, varchar; p(id)]
T[ar_internal_metadata; 2000; "key", varchar; "value", varchar; created_at, datetime; updated_at, datetime; p(id)]
T[friendly_id_slugs; 2000; id, integer; slug, varchar; sluggable_id, integer; sluggable_type, varchar; "scope", varchar; created_at, datetime; deleted_at, datetime; p(id)]
T[schema_migrations; 2000; version, varchar; p(id)]
T[spree_addresses; 2000; id, integer; firstname, varchar; lastname, varchar; address1, varchar; address2, varchar; city, varchar; zipcode, varchar; phone, varchar; state_name, varchar; alternative_phone, varchar; company, varchar; state_id, integer; country_id, integer; created_at, datetime; updated_at, datetime; user_id, integer; deleted_at, datetime; label, varchar; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_adjustments; 2000; id, integer; source_type, varchar; source_id, integer; adjustable_type, varchar; adjustable_id, integer; amount, decimal; label, varchar; mandatory, bool; eligible, bool; created_at, datetime; updated_at, datetime; "state", varchar; order_id, integer; included, bool; p(id)]
T[spree_assets; 2000; id, integer; viewable_type, varchar; viewable_id, integer; attachment_width, integer; attachment_height, integer; attachment_file_size, integer; "position", integer; attachment_content_type, varchar; attachment_file_name, varchar; type, varchar; attachment_updated_at, datetime; alt, varchar; created_at, datetime; updated_at, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_calculators; 2000; id, integer; type, varchar; calculable_type, varchar; calculable_id, integer; created_at, datetime; updated_at, datetime; preferences, varchar; deleted_at, datetime; p(id)]
T[spree_checks; 2000; id, integer; payment_method_id, integer; user_id, integer; account_holder_name, varchar; account_holder_type, varchar; routing_number, varchar; account_number, varchar; account_type, varchar; status, varchar; last_digits, varchar; gateway_customer_profile_id, varchar; gateway_payment_profile_id, varchar; created_at, datetime; updated_at, datetime; deleted_at, datetime; p(id)]
T[spree_cms_pages; 2000; id, integer; title, varchar; meta_title, varchar; content, varchar; meta_description, varchar; visible, bool; slug, varchar; type, varchar; locale, varchar; deleted_at, datetime; store_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_cms_sections; 2000; id, integer; name, varchar; content, varchar; settings, varchar; fit, varchar; destination, varchar; type, varchar; "position", integer; linked_resource_type, varchar; linked_resource_id, integer; cms_page_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_countries; 2000; id, integer; iso_name, varchar; iso, varchar; iso3, varchar; name, varchar; numcode, integer; states_required, bool; updated_at, datetime; zipcode_required, bool; created_at, datetime; p(id)]
T[spree_credit_cards; 2000; id, integer; "month", varchar; "year", varchar; cc_type, varchar; last_digits, varchar; address_id, integer; gateway_customer_profile_id, varchar; gateway_payment_profile_id, varchar; created_at, datetime; updated_at, datetime; name, varchar; user_id, integer; payment_method_id, integer; "default", bool; deleted_at, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_customer_returns; 2000; id, integer; number, varchar; stock_location_id, integer; created_at, datetime; updated_at, datetime; store_id, integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_digital_links; 2000; id, integer; digital_id, integer; line_item_id, integer; token, varchar; access_counter, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_digitals; 2000; id, integer; variant_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_gateways; 2000; id, integer; type, varchar; name, varchar; description, varchar; active, bool; environment, varchar; server, varchar; test_mode, bool; created_at, datetime; updated_at, datetime; preferences, varchar; p(id)]
T[spree_inventory_units; 2000; id, integer; "state", varchar; variant_id, integer; order_id, integer; shipment_id, integer; created_at, datetime; updated_at, datetime; pending, bool; line_item_id, integer; quantity, integer; original_return_item_id, integer; p(id)]
T[spree_line_items; 2000; id, integer; variant_id, integer; order_id, integer; quantity, integer; price, decimal; created_at, datetime; updated_at, datetime; currency, varchar; cost_price, decimal; tax_category_id, integer; adjustment_total, decimal; additional_tax_total, decimal; promo_total, decimal; included_tax_total, decimal; pre_tax_amount, decimal; taxable_adjustment_total, decimal; non_taxable_adjustment_total, decimal; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_log_entries; 2000; id, integer; source_type, varchar; source_id, integer; details, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_menu_items; 2000; id, integer; name, varchar; subtitle, varchar; destination, varchar; new_window, bool; item_type, varchar; linked_resource_type, varchar; linked_resource_id, integer; code, varchar; parent_id, integer; lft, integer; rgt, integer; depth, integer; menu_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_menus; 2000; id, integer; name, varchar; location, varchar; locale, varchar; store_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_oauth_access_grants; 2000; id, integer; resource_owner_id, integer; application_id, integer; token, varchar; expires_in, integer; redirect_uri, varchar; created_at, datetime; revoked_at, datetime; scopes, varchar; resource_owner_type, varchar; p(id)]
T[spree_oauth_access_tokens; 2000; id, integer; resource_owner_id, integer; application_id, integer; token, varchar; refresh_token, varchar; expires_in, integer; revoked_at, datetime; created_at, datetime; scopes, varchar; previous_refresh_token, varchar; resource_owner_type, varchar; p(id)]
T[spree_oauth_applications; 2000; id, integer; name, varchar; uid, varchar; secret, varchar; redirect_uri, varchar; scopes, varchar; confidential, bool; created_at, datetime; updated_at, datetime; p(id)]
T[spree_option_type_prototypes; 2000; id, integer; prototype_id, integer; option_type_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_option_types; 2000; id, integer; name, varchar; presentation, varchar; "position", integer; created_at, datetime; updated_at, datetime; filterable, bool; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_option_value_variants; 2000; id, integer; variant_id, integer; option_value_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_option_values; 2000; id, integer; "position", integer; name, varchar; presentation, varchar; option_type_id, integer; created_at, datetime; updated_at, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_order_promotions; 2000; id, integer; order_id, integer; promotion_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_orders; 2000; id, integer; number, varchar; item_total, decimal; total, decimal; "state", varchar; adjustment_total, decimal; user_id, integer; completed_at, datetime; bill_address_id, integer; ship_address_id, integer; payment_total, decimal; shipment_state, varchar; payment_state, varchar; email, varchar; special_instructions, varchar; created_at, datetime; updated_at, datetime; currency, varchar; last_ip_address, varchar; created_by_id, integer; shipment_total, decimal; additional_tax_total, decimal; promo_total, decimal; channel, varchar; included_tax_total, decimal; item_count, integer; approver_id, integer; approved_at, datetime; confirmation_delivered, bool; considered_risky, bool; token, varchar; canceled_at, datetime; canceler_id, integer; store_id, integer; state_lock_version, integer; taxable_adjustment_total, decimal; non_taxable_adjustment_total, decimal; store_owner_notification_delivered, bool; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_payment_capture_events; 2000; id, integer; amount, decimal; payment_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_payment_methods; 2000; id, integer; type, varchar; name, varchar; description, varchar; active, bool; deleted_at, datetime; created_at, datetime; updated_at, datetime; display_on, varchar; auto_capture, bool; preferences, varchar; "position", integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_payment_methods_stores; 2000; payment_method_id, integer; store_id, integer; p(id)]
T[spree_payments; 2000; id, integer; amount, decimal; order_id, integer; source_type, varchar; source_id, integer; payment_method_id, integer; "state", varchar; response_code, varchar; avs_response, varchar; created_at, datetime; updated_at, datetime; number, varchar; cvv_response_code, varchar; cvv_response_message, varchar; public_metadata, varchar; private_metadata, varchar; intent_client_key, varchar; p(id)]
T[spree_preferences; 2000; id, integer; "value", varchar; "key", varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_prices; 2000; id, integer; variant_id, integer; amount, decimal; currency, varchar; deleted_at, datetime; created_at, datetime; updated_at, datetime; compare_at_amount, decimal; p(id)]
T[spree_product_option_types; 2000; id, integer; "position", integer; product_id, integer; option_type_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_product_promotion_rules; 2000; id, integer; product_id, integer; promotion_rule_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_product_properties; 2000; id, integer; "value", varchar; product_id, integer; property_id, integer; created_at, datetime; updated_at, datetime; "position", integer; show_property, bool; filter_param, varchar; p(id)]
T[spree_products; 2000; id, integer; name, varchar; description, varchar; available_on, datetime; deleted_at, datetime; slug, varchar; meta_description, varchar; meta_keywords, varchar; tax_category_id, integer; shipping_category_id, integer; created_at, datetime; updated_at, datetime; promotionable, bool; meta_title, varchar; discontinue_on, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_products_stores; 2000; id, integer; product_id, integer; store_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_products_taxons; 2000; id, integer; product_id, integer; taxon_id, integer; "position", integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_promotion_action_line_items; 2000; id, integer; promotion_action_id, integer; variant_id, integer; quantity, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_promotion_actions; 2000; id, integer; promotion_id, integer; "position", integer; type, varchar; deleted_at, datetime; created_at, datetime; updated_at, datetime; p(id)]
T[spree_promotion_categories; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; code, varchar; p(id)]
T[spree_promotion_rule_taxons; 2000; id, integer; taxon_id, integer; promotion_rule_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_promotion_rule_users; 2000; id, integer; user_id, integer; promotion_rule_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_promotion_rules; 2000; id, integer; promotion_id, integer; user_id, integer; product_group_id, integer; type, varchar; created_at, datetime; updated_at, datetime; code, varchar; preferences, varchar; p(id)]
T[spree_promotions; 2000; id, integer; description, varchar; expires_at, datetime; starts_at, datetime; name, varchar; type, varchar; usage_limit, integer; match_policy, varchar; code, varchar; advertise, bool; "path", varchar; created_at, datetime; updated_at, datetime; promotion_category_id, integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_promotions_stores; 2000; id, integer; promotion_id, integer; store_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_properties; 2000; id, integer; name, varchar; presentation, varchar; created_at, datetime; updated_at, datetime; filterable, bool; filter_param, varchar; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_property_prototypes; 2000; id, integer; prototype_id, integer; property_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_prototype_taxons; 2000; id, integer; taxon_id, integer; prototype_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_prototypes; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_refund_reasons; 2000; id, integer; name, varchar; active, bool; mutable, bool; created_at, datetime; updated_at, datetime; p(id)]
T[spree_refunds; 2000; id, integer; payment_id, integer; amount, decimal; transaction_id, varchar; created_at, datetime; updated_at, datetime; refund_reason_id, integer; reimbursement_id, integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_reimbursement_credits; 2000; id, integer; amount, decimal; reimbursement_id, integer; creditable_id, integer; creditable_type, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_reimbursement_types; 2000; id, integer; name, varchar; active, bool; mutable, bool; created_at, datetime; updated_at, datetime; type, varchar; p(id)]
T[spree_reimbursements; 2000; id, integer; number, varchar; reimbursement_status, varchar; customer_return_id, integer; order_id, integer; total, decimal; created_at, datetime; updated_at, datetime; p(id)]
T[spree_return_authorization_reasons; 2000; id, integer; name, varchar; active, bool; mutable, bool; created_at, datetime; updated_at, datetime; p(id)]
T[spree_return_authorizations; 2000; id, integer; number, varchar; "state", varchar; order_id, integer; memo, varchar; created_at, datetime; updated_at, datetime; stock_location_id, integer; return_authorization_reason_id, integer; p(id)]
T[spree_return_items; 2000; id, integer; return_authorization_id, integer; inventory_unit_id, integer; exchange_variant_id, integer; created_at, datetime; updated_at, datetime; pre_tax_amount, decimal; included_tax_total, decimal; additional_tax_total, decimal; reception_status, varchar; acceptance_status, varchar; customer_return_id, integer; reimbursement_id, integer; acceptance_status_errors, varchar; preferred_reimbursement_type_id, integer; override_reimbursement_type_id, integer; resellable, bool; p(id)]
T[spree_role_users; 2000; id, integer; role_id, integer; user_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_roles; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_shipments; 2000; id, integer; tracking, varchar; number, varchar; cost, decimal; shipped_at, datetime; order_id, integer; address_id, integer; "state", varchar; created_at, datetime; updated_at, datetime; stock_location_id, integer; adjustment_total, decimal; additional_tax_total, decimal; promo_total, decimal; included_tax_total, decimal; pre_tax_amount, decimal; taxable_adjustment_total, decimal; non_taxable_adjustment_total, decimal; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_shipping_categories; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_shipping_method_categories; 2000; id, integer; shipping_method_id, integer; shipping_category_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_shipping_method_zones; 2000; id, integer; shipping_method_id, integer; zone_id, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_shipping_methods; 2000; id, integer; name, varchar; display_on, varchar; deleted_at, datetime; created_at, datetime; updated_at, datetime; tracking_url, varchar; admin_name, varchar; tax_category_id, integer; code, varchar; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_shipping_rates; 2000; id, integer; shipment_id, integer; shipping_method_id, integer; selected, bool; cost, decimal; created_at, datetime; updated_at, datetime; tax_rate_id, integer; p(id)]
T[spree_state_changes; 2000; id, integer; name, varchar; previous_state, varchar; stateful_id, integer; user_id, integer; stateful_type, varchar; next_state, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_states; 2000; id, integer; name, varchar; abbr, varchar; country_id, integer; updated_at, datetime; created_at, datetime; p(id)]
T[spree_stock_items; 2000; id, integer; stock_location_id, integer; variant_id, integer; count_on_hand, integer; created_at, datetime; updated_at, datetime; backorderable, bool; deleted_at, datetime; p(id)]
T[spree_stock_locations; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; "default", bool; address1, varchar; address2, varchar; city, varchar; state_id, integer; state_name, varchar; country_id, integer; zipcode, varchar; phone, varchar; active, bool; backorderable_default, bool; propagate_all_variants, bool; admin_name, varchar; p(id)]
T[spree_stock_movements; 2000; id, integer; stock_item_id, integer; quantity, integer; action, varchar; created_at, datetime; updated_at, datetime; originator_type, varchar; originator_id, integer; p(id)]
T[spree_stock_transfers; 2000; id, integer; type, varchar; reference, varchar; source_location_id, integer; destination_location_id, integer; created_at, datetime; updated_at, datetime; number, varchar; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_store_credit_categories; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_store_credit_events; 2000; id, integer; store_credit_id, integer; action, varchar; amount, decimal; authorization_code, varchar; user_total_amount, decimal; originator_id, integer; originator_type, varchar; deleted_at, datetime; created_at, datetime; updated_at, datetime; p(id)]
T[spree_store_credit_types; 2000; id, integer; name, varchar; priority, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_store_credits; 2000; id, integer; user_id, integer; category_id, integer; created_by_id, integer; amount, decimal; amount_used, decimal; memo, varchar; deleted_at, datetime; currency, varchar; amount_authorized, decimal; originator_id, integer; originator_type, varchar; type_id, integer; created_at, datetime; updated_at, datetime; store_id, integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_stores; 2000; id, integer; name, varchar; url, varchar; meta_description, varchar; meta_keywords, varchar; seo_title, varchar; mail_from_address, varchar; default_currency, varchar; code, varchar; "default", bool; created_at, datetime; updated_at, datetime; supported_currencies, varchar; facebook, varchar; twitter, varchar; instagram, varchar; default_locale, varchar; customer_support_email, varchar; default_country_id, integer; description, varchar; address, varchar; contact_phone, varchar; new_order_notifications_email, varchar; checkout_zone_id, integer; seo_robots, varchar; supported_locales, varchar; deleted_at, datetime; settings, varchar; p(id)]
T[spree_tax_categories; 2000; id, integer; name, varchar; description, varchar; is_default, bool; deleted_at, datetime; created_at, datetime; updated_at, datetime; tax_code, varchar; p(id)]
T[spree_tax_rates; 2000; id, integer; amount, decimal; zone_id, integer; tax_category_id, integer; included_in_price, bool; created_at, datetime; updated_at, datetime; name, varchar; show_rate_in_label, bool; deleted_at, datetime; p(id)]
T[spree_taxonomies; 2000; id, integer; name, varchar; created_at, datetime; updated_at, datetime; "position", integer; store_id, integer; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_taxons; 2000; id, integer; parent_id, integer; "position", integer; name, varchar; permalink, varchar; taxonomy_id, integer; lft, integer; rgt, integer; description, varchar; created_at, datetime; updated_at, datetime; meta_title, varchar; meta_description, varchar; meta_keywords, varchar; depth, integer; hide_from_nav, bool; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_trackers; 2000; id, integer; analytics_id, varchar; active, bool; created_at, datetime; updated_at, datetime; engine, integer; p(id)]
T[spree_users; 2000; id, integer; encrypted_password, varchar; password_salt, varchar; email, varchar; remember_token, varchar; persistence_token, varchar; reset_password_token, varchar; perishable_token, varchar; sign_in_count, integer; failed_attempts, integer; last_request_at, datetime; current_sign_in_at, datetime; last_sign_in_at, datetime; current_sign_in_ip, varchar; last_sign_in_ip, varchar; login, varchar; ship_address_id, integer; bill_address_id, integer; authentication_token, varchar; unlock_token, varchar; locked_at, datetime; reset_password_sent_at, datetime; created_at, datetime; updated_at, datetime; public_metadata, varchar; private_metadata, varchar; spree_api_key, varchar; remember_created_at, datetime; deleted_at, datetime; confirmation_token, varchar; confirmed_at, datetime; confirmation_sent_at, datetime; p(id)]
T[spree_variants; 2000; id, integer; sku, varchar; weight, decimal; height, decimal; width, decimal; depth, decimal; deleted_at, datetime; is_master, bool; product_id, integer; cost_price, decimal; "position", integer; cost_currency, varchar; track_inventory, bool; tax_category_id, integer; updated_at, datetime; discontinue_on, datetime; created_at, datetime; public_metadata, varchar; private_metadata, varchar; p(id)]
T[spree_webhooks_events; 2000; id, integer; execution_time, integer; name, varchar; request_errors, varchar; response_code, varchar; subscriber_id, integer; success, bool; url, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_webhooks_subscribers; 2000; id, integer; url, varchar; active, bool; subscriptions, varchar; created_at, datetime; updated_at, datetime; p(id)]
T[spree_wished_items; 2000; id, integer; variant_id, integer; wishlist_id, integer; quantity, integer; created_at, datetime; updated_at, datetime; p(id)]
T[spree_wishlists; 2000; id, integer; user_id, integer; store_id, integer; name, varchar; token, varchar; is_private, bool; is_default, bool; created_at, datetime; updated_at, datetime; p(id)]
T[spree_zone_members; 2000; id, integer; zoneable_type, varchar; zoneable_id, integer; zone_id, integer; created_at, datetime; updated_at, datetime; p(id)]
D[spree_adjustments.state; 0; 15; 100; ('open', 'closed'); ]
D[spree_assets.type; 0; 15; 100; ('Spree::Asset', 'Spree::Icon'); ]
D[spree_assets.viewable_type; 0; 15; 100; ('Spree::MenuItem', 'Spree::Taxon', 'Spree::Variant'); ]
D[spree_inventory_units.state; 0; 15; 100; ('on_hand', 'backordered', 'on', 'fill_backorder', 'do', 'fulfill_order', 'shipped', 'returned'); ]
D[spree_menus.location; 0; 15; 100; (); ]
D[spree_menu_items.item_type; 0; 15; 100; (); ]
D[spree_menu_items.linked_resource_type; 0; 15; 100; (); ]
D[spree_orders.payment_state; 0; 15; 100; (); ]
D[spree_orders.shipment_state; 0; 15; 100; (); ]
D[spree_payments.state; 0; 15; 100; ('checkout', 'pending', 'completed', 'processing', 'failed', 'do', 'after_completed', 'void', 'after_void', 'invalid'); ]
D[spree_payments.source_type; 0; 15; 100; ('Spree::CreditCard', 'Spree::Payment', 'Spree::Refund'); ]
D[spree_reimbursements.reimbursement_status; 0; 15; 100; ('errored', 'pending', 'reimbursed'); ]
D[spree_reimbursement_types.type; 0; 15; 100; ('Spree::ReimbursementType', 'Spree::ReimbursementType::Credit', 'Spree::ReimbursementType::Exchange', 'Spree::ReimbursementType::OriginalPayment', 'Spree::ReimbursementType::StoreCredit'); ]
D[spree_return_authorizations.state; 0; 15; 100; ('canceled', 'do', 'cancel_return_items', 'authorized'); ]
D[spree_return_items.reception_status; 0; 15; 100; ('received', 'do', 'attempt_accept', 'process_inventory_unit!', 'awaiting', 'cancelled', 'given_to_customer'); ]
D[spree_return_items.acceptance_status; 0; 15; 100; ('accepted', 'pending', 'manual_intervention_required', 'rejected', 'do', 'persist_acceptance_status_errors'); ]
D[spree_shipments.state; 0; 15; 100; ('pending', 'ready', 'canceled', 'shipped', 'do', 'after_ship', 'after_cancel', 'after_resume'); ]
D[spree_state_changes.stateful_type; 0; 15; 100; ('Spree::Order', 'Spree::Payment', 'Spree::Shipment'); ]
D[spree_store_credit_events.originator_type; 0; 15; 100; ('Spree::StockTransfer'); ]
D[spree_taxons.hide_from_nav; 0; 15; 100; (); ]
